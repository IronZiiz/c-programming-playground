/*Inicio do estudo em while*/

#include <stdio.h>
    main (){ 
        int contador , total; 

        while (total <=10 ){  
        contador++ ; total+=1; 
         }

        printf ("Total: %d", total); 
    return 0; 
    }