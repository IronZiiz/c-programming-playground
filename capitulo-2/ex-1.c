/*Atribuir valores a variaveis e ver se eles são iguais */
#include <stdio.h> 
main () {
    /*Declaração de variaveis*/
    int a, b ; 
    printf("Atribua dois valores:\n"); 
    scanf ("%d%d", &a,&b); 

    if ( a == b)
    printf ("são iguais");


return 0; 
}
